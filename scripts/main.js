/* On isole ici l'appel de la fonction pour exécuter l'ensemble des fonctions créées dans les différents fichiers */
/* lancerJeu() */

/* Sélectionnez avec la méthode getElementById : l’input dans lequel le joueur va écrire son texte ;
le bouton de validation. */
let baliseEcritureZoneSaisie = document.getElementById("#inputEcriture");
console.log(baliseEcritureZoneSaisie)
